// Find the ASCII value of a character Programs

const character= prompt('Enter a character: ');

// convert into ASCII value
const res = character.charCodeAt(0);

console.log("The ASCII value is: " , res);